[package]
authors = ["Cameron Eldridge <cameldridge@gmail.com>"]
edition = "2018"
name = "server"
version = "0.1.0"

[dependencies]
shared = { path = "../shared" }
data = { path = "../data" }

anyhow = "1.0"
dotenv = "0.15"
env_logger = "0.7"
juniper = { git = "https://github.com/foxfriends/juniper.git", branch = "master", default-features = false, features = ["chrono", "uuid"] }
juniper_rocket = { git = "https://github.com/foxfriends/juniper.git", branch = "master" }
log = "0.4"
rocket = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "0.8", features = ["serde", "v4"] }
